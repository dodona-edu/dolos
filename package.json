{
  "name": "dolos",
  "version": "0.0.1",
  "description": "Plagiarism detection for Dodona based on the Winnowing algorithm",
  "main": "dist/app.js",
  "scripts": {
    "start": "tsc && node dist/app.js",
    "test-performance": "tsc && time head -c 1000000 /dev/random | node dist/app.js > /dev/null",
    "test": "web-scripts test --coverage",
    "build": "tsc",
    "lint": "tslint 'src/**/*.ts'"
  },
  "repository": "git@github.com:dodona-edu/dolos.git",
  "author": "Bart Mesuere <Bart.Mesuere@UGent.be>",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "@spotify/web-scripts": "^1.1.0",
    "@types/node": "^12.0.10",
    "tslint": "^5.18.0",
    "typescript": "^3.5.2"
  },
  "dependencies": {
    "nprime": "^0.2.1"
  }
}
